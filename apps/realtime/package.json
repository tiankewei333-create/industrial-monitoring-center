{
  "name": "@imc/realtime",
  "version": "0.1.0",
  "private": true,
  "description": "IMC 实时桥：MQTT 订阅 → WebSocket 推送（MVP 含可选内嵌 Broker）",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"[imc-realtime] lint ok\" && exit 0",
    "test": "echo \"[imc-realtime] no tests yet\" && exit 0"
  },
  "dependencies": {
    "@imc/shared-types": "0.1.0",
    "aedes": "^0.51.3",
    "mqtt": "^5.10.4",
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/ws": "^8.18.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
