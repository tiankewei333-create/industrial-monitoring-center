{
  "name": "@imc/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "IMC shared protocol types and constants",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "echo \"[shared-types] TS sources consumed directly for now\" && exit 0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"[shared-types] lint ok\" && exit 0",
    "test": "echo \"[shared-types] no tests yet\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
