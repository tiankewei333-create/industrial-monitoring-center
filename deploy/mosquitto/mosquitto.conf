# Used only when running Mosquitto without the Compose entrypoint
# (anonymous lab broker). Compose MVP generates an authenticated
# config at runtime: allow_anonymous false + password_file.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
