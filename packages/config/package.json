{
  "name": "@imc/config",
  "version": "0.1.0",
  "private": true,
  "description": "IMC 共享配置（端口、主题、默认阈值）",
  "main": "index.js",
  "scripts": {
    "build": "echo \"[config] noop\" && exit 0",
    "lint": "echo \"[config] lint placeholder\" && exit 0",
    "typecheck": "echo \"[config] typecheck placeholder\" && exit 0",
    "test": "echo \"[config] no tests yet\" && exit 0"
  }
}
