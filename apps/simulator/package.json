{
  "name": "@imc/simulator",
  "version": "0.1.0",
  "private": true,
  "description": "IMC line simulator publishing Machine001 telemetry",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "dev:hot": "tsx watch src/index.ts --force-temp=86",
    "dev:spike": "tsx watch src/index.ts --spike --spike-sec=15 --cool-sec=20",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"[imc-simulator] lint ok\" && exit 0",
    "test": "echo \"[imc-simulator] no tests yet\" && exit 0"
  },
  "dependencies": {
    "@imc/shared-types": "0.1.0",
    "mqtt": "^5.10.4"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
