# Placeholder — apps not containerized for MVP local pipeline yet.
# Local path: npm run dev:live (embedded MQTT in realtime).
FROM node:20-alpine
WORKDIR /app
CMD ["sh", "-c", "echo '[imc-realtime] use npm run dev:realtime locally'; sleep infinity"]
